

export interface Excercise {
    idex: number;
    title: String;
    description: String;
    restInterval: number;
    repetitions: number;
    addedWeight: number
    // health data goes here, once that has been defined, for now not nessesary
}