import { ReactElement } from "react";
import { HealthData } from "../utils/types";



interface Props {
    healthData: HealthData,
}

export function HealthSummary({healthData} : Props): ReactElement {


    return (
        <div></div>
    );
}